# This file was previously used for an nginx-based Docker setup.
# The current Dockerfile uses a lightweight Node-based static server
# (http-server) instead of nginx to keep the project aligned with
# the course scope and avoid requiring nginx configuration knowledge.
# The file is intentionally retained for reference only.
